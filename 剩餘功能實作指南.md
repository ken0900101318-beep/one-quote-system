# 剩餘功能實作指南

## 已完成 ✅
1. ✅ 刪除專案
2. ✅ 報價中刪除項目
3. ✅ 報價中新增項目

---

## 待實作（4 個功能）

### 功能 4：刪除施工進度記錄 🟡 中優先級

**需求**：誤記錄的施工進度需要能夠刪除

**實作位置**：project.html - 施工進度顯示區

**修改內容**：

1. **施工進度表格加入操作欄**：
```html
<!-- 在 project.html 的施工進度表格中 -->
<thead>
    <tr>
        <th>項目</th>
        <th>進度</th>
        <th>狀態</th>
        <th>更新日期</th>
        <th>操作</th>  <!-- 新增 -->
    </tr>
</thead>
```

2. **每一行加入刪除按鈕**：
```javascript
<td>
    <button onclick="deleteProgress('${project.id}', ${index})" 
            class="btn btn-sm" 
            style="padding: 5px 10px; background: #ef4444; color: white;">
        🗑️ 刪除
    </button>
</td>
```

3. **加入刪除函數**：
```javascript
function deleteProgress(projectId, progressIndex) {
    if (!confirm('確定要刪除此進度記錄嗎？')) return;
    
    const projects = JSON.parse(localStorage.getItem('projects') || '[]');
    const project = projects.find(p => p.id === projectId);
    
    if (!project || !project.progress) return;
    
    const deletedItem = project.progress[progressIndex];
    project.progress.splice(progressIndex, 1);
    
    // 記錄到時間軸
    if (!project.statusLogs) project.statusLogs = [];
    project.statusLogs.push({
        date: new Date().toISOString().split('T')[0],
        status: '刪除進度記錄',
        operator: JSON.parse(localStorage.getItem('currentUser')).id,
        note: `刪除「${deletedItem.item}」進度記錄`
    });
    
    localStorage.setItem('projects', JSON.stringify(projects));
    
    alert('✅ 進度記錄已刪除！');
    location.reload();
}
```

---

### 功能 5：刪除報價版本 🟡 中優先級

**需求**：錯誤的報價版本需要能夠刪除

**實作位置**：project.html - 報價版本顯示區

**注意**：
- ⚠️ 不能刪除 v1（原始報價）
- ⚠️ 不能刪除最新版本（當前使用的版本）
- ✅ 只能刪除中間的錯誤版本

**修改內容**：

1. **報價版本區塊加入刪除按鈕**：
```javascript
// 在 project.html 顯示報價版本的地方
quoteVersionsHtml = project.quoteVersions.map((ver, index) => {
    const canDelete = index > 0 && index < project.quoteVersions.length - 1;
    
    return `
        <div style="padding: 10px; border: 1px solid #e5e7eb; border-radius: 4px; margin-bottom: 10px;">
            <strong>${ver.version}</strong> - ${ver.date} - ${ver.operator}
            <br>總額：$${ver.totalAmount.toLocaleString()}
            <br>原因：${ver.reason}
            ${canDelete ? `
                <button onclick="deleteQuoteVersion('${project.id}', ${index})" 
                        style="padding: 3px 8px; background: #ef4444; color: white; border: none; border-radius: 4px; cursor: pointer; margin-top: 5px;">
                    🗑️ 刪除此版本
                </button>
            ` : ''}
        </div>
    `;
}).join('');
```

2. **加入刪除函數**：
```javascript
function deleteQuoteVersion(projectId, versionIndex) {
    const projects = JSON.parse(localStorage.getItem('projects') || '[]');
    const project = projects.find(p => p.id === projectId);
    
    if (!project || !project.quoteVersions) return;
    
    // 防呆檢查
    if (versionIndex === 0) {
        alert('❌ 不能刪除原始報價（v1）！');
        return;
    }
    
    if (versionIndex === project.quoteVersions.length - 1) {
        alert('❌ 不能刪除最新版本！請先建立新版本再刪除此版本。');
        return;
    }
    
    const deletedVersion = project.quoteVersions[versionIndex];
    
    if (!confirm(`確定要刪除「${deletedVersion.version}」嗎？\n\n原因：${deletedVersion.reason}\n總額：$${deletedVersion.totalAmount.toLocaleString()}\n\n⚠️ 刪除後無法復原！`)) {
        return;
    }
    
    project.quoteVersions.splice(versionIndex, 1);
    
    // 重新編號版本
    project.quoteVersions.forEach((ver, idx) => {
        ver.version = 'v' + (idx + 1);
    });
    
    // 記錄到時間軸
    if (!project.statusLogs) project.statusLogs = [];
    project.statusLogs.push({
        date: new Date().toISOString().split('T')[0],
        status: '刪除報價版本',
        operator: JSON.parse(localStorage.getItem('currentUser')).id,
        note: `刪除報價版本（原 ${deletedVersion.version}），原因：${deletedVersion.reason}`
    });
    
    localStorage.setItem('projects', JSON.stringify(projects));
    
    alert('✅ 報價版本已刪除！版本編號已重新整理。');
    location.reload();
}
```

---

### 功能 6：使用者管理 🟢 低優先級

**需求**：新增/編輯/刪除員工帳號

**實作方式**：創建新頁面 `users.html`

**檔案結構**：
```
users.html（使用者管理頁面）
  ├── 使用者列表表格
  ├── 新增使用者按鈕
  ├── 新增/編輯 Modal
  └── 刪除功能
```

**功能清單**：
1. ✅ 列出所有使用者
2. ✅ 新增使用者（帳號、姓名、角色、密碼）
3. ✅ 編輯使用者（修改姓名、角色）
4. ✅ 刪除使用者（不能刪除自己）
5. ✅ 重設密碼

**導航列加入連結**：
```html
<!-- 在 dashboard.html, project.html 等頁面的導航列加入 -->
${currentUser.role === 'admin' ? `
    <button class="btn" onclick="location.href='users.html'" style="margin-left: 10px;">
        👥 使用者管理
    </button>
` : ''}
```

**完整頁面代碼**：（約 400 行，需要完整創建）

由於代碼量較大，建議：
1. 複製 prices.html 結構
2. 修改為使用者管理邏輯
3. 資料儲存到 localStorage 的 'users'

---

### 功能 7：回復報價版本 🟢 低優先級

**需求**：誤改報價後，可以回復到舊版本

**實作位置**：project.html - 報價版本顯示區

**修改內容**：

1. **每個版本加入「回復」按鈕**：
```javascript
quoteVersionsHtml = project.quoteVersions.map((ver, index) => {
    const isCurrent = index === project.quoteVersions.length - 1;
    
    return `
        <div style="padding: 10px; border: 1px solid #e5e7eb; border-radius: 4px; margin-bottom: 10px; ${isCurrent ? 'background: #f0fdf4; border-color: #059669;' : ''}">
            <strong>${ver.version}</strong> ${isCurrent ? '<span style="color: #059669;">（當前版本）</span>' : ''}
            <br>日期：${ver.date}
            <br>總額：$${ver.totalAmount.toLocaleString()}
            <br>原因：${ver.reason}
            ${!isCurrent ? `
                <button onclick="restoreQuoteVersion('${project.id}', ${index})" 
                        style="padding: 5px 12px; background: #3b82f6; color: white; border: none; border-radius: 4px; cursor: pointer; margin-top: 5px;">
                    🔄 回復到此版本
                </button>
            ` : ''}
        </div>
    `;
}).join('');
```

2. **加入回復函數**：
```javascript
function restoreQuoteVersion(projectId, versionIndex) {
    const projects = JSON.parse(localStorage.getItem('projects') || '[]');
    const project = projects.find(p => p.id === projectId);
    
    if (!project || !project.quoteVersions) return;
    
    const targetVersion = project.quoteVersions[versionIndex];
    
    if (!confirm(`確定要回復到「${targetVersion.version}」嗎？\n\n日期：${targetVersion.date}\n總額：$${targetVersion.totalAmount.toLocaleString()}\n原因：${targetVersion.reason}\n\n這會建立一個新版本（v${project.quoteVersions.length + 1}），保留當前報價的歷史記錄。`)) {
        return;
    }
    
    // 建立新版本（複製舊版本的內容）
    const newVersion = {
        version: 'v' + (project.quoteVersions.length + 1),
        date: new Date().toISOString().split('T')[0],
        totalAmount: targetVersion.totalAmount,
        operator: JSON.parse(localStorage.getItem('currentUser')).id,
        reason: `回復到 ${targetVersion.version}`,
        note: `回復報價內容（原版本：${targetVersion.version}，日期：${targetVersion.date}）`,
        items: JSON.parse(JSON.stringify(targetVersion.items)) // 深拷貝
    };
    
    project.quoteVersions.push(newVersion);
    project.totalAmount = newVersion.totalAmount;
    
    // 記錄到時間軸
    if (!project.statusLogs) project.statusLogs = [];
    project.statusLogs.push({
        date: newVersion.date,
        status: '回復報價版本',
        operator: newVersion.operator,
        note: `回復到 ${targetVersion.version}（${targetVersion.date}），總額：$${newVersion.totalAmount.toLocaleString()}`
    });
    
    localStorage.setItem('projects', JSON.stringify(projects));
    
    alert(`✅ 已回復到 ${targetVersion.version}！\n\n建立新版本：${newVersion.version}\n總額：$${newVersion.totalAmount.toLocaleString()}`);
    location.reload();
}
```

---

## 實作優先級建議

### 📍 **立即實作（建議）**：
- ✅ 功能 1-3（已完成）

### 🔶 **短期實作（本週內）**：
- 功能 4：刪除施工進度（5 分鐘）
- 功能 5：刪除報價版本（10 分鐘）
- 功能 7：回復報價版本（10 分鐘）

### 🔷 **中期實作（未來 1-2 週）**：
- 功能 6：使用者管理（需要新建完整頁面，約 1-2 小時）

---

## 測試檢查清單

實作完成後，請測試：

### 功能 4（刪除進度）
- [ ] 能看到刪除按鈕
- [ ] 點擊後有確認對話框
- [ ] 刪除後進度消失
- [ ] 時間軸有記錄

### 功能 5（刪除版本）
- [ ] v1 無法刪除
- [ ] 最新版本無法刪除
- [ ] 中間版本可以刪除
- [ ] 刪除後版本重新編號
- [ ] 時間軸有記錄

### 功能 6（使用者管理）
- [ ] 能列出所有使用者
- [ ] 能新增使用者
- [ ] 能編輯使用者
- [ ] 能刪除使用者（不能刪自己）
- [ ] 新使用者能登入

### 功能 7（回復版本）
- [ ] 能看到回復按鈕
- [ ] 當前版本無回復按鈕
- [ ] 回復後建立新版本
- [ ] 報價內容正確回復
- [ ] 時間軸有記錄

---

## 需要我立即實作哪些功能？

**選項 A**：功能 4、5、7（約 25 分鐘）
**選項 B**：全部實作（功能 4-7，約 2 小時）
**選項 C**：暫緩，先測試前 3 個功能

請告訴我你的選擇！
